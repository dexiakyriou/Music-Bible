Feature: buy track

	# POST /tracks
	# GET, PUT, DELETE /products/{track_id}

	Scenario: happy
		When I buy a track by it's name
		Then I should see it's description
    And I read the message 
	
    Scenario: unhappy
	  When I don't buy a track by it's name
		Then I should see the deleted product
